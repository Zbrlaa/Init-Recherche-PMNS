#include <stdio.h>
#include <stdlib.h>
#include <stdint.h> //Pour utiliser int64_t
#include "param.h"

void genpoly(int n){

}

void MONTGMUL(){
	
}

int main(){
	//Polynome sous forme de tableau d'entier avec L[0] le degré 0
	for(int i; i<100; i++){
		// A = rand();
		// printf(A);

		// B = rand();
		// printf(B);

		// MONTGMULL(Cprime,A,B);
		// printf(Cprime)
	}
}
